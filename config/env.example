# Copy this file to .env in the project root and fill in your credentials
# NEVER commit the actual .env file

DB_USER=your_db_username
DB_PASSWORD=your_db_password
ENV=local          # Options: local | rds
