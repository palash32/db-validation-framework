# Database connection configuration
# Credentials (DB_USER, DB_PASSWORD) are loaded from environment variables / .env

local:
  host: "127.0.0.1"
  port: 3306
  database: "test_hrms"

rds:
  host: "your-rds-instance.xxxxxxxxxxxx.us-east-1.rds.amazonaws.com"
  port: 3306
  database: "hrms_prod"
